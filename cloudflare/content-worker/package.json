{
  "name": "@lucas-camargo/content-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "wrangler deploy --dry-run --outdir dist",
    "check": "yarn lint && yarn test && yarn build",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "5.9.2",
    "vitest": "4.1.10",
    "wrangler": "4.112.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "yarn@1.22.22"
}
