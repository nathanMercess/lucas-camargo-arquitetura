export interface SiteContact {
  readonly email: string;
  readonly phoneLabel: string;
  readonly phoneE164: string;
  readonly instagramUrl: string;
  readonly whatsappNumber: string;
  readonly whatsappDefaultMessage: string;
}
